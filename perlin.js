let numhash = function(){
    //takes arbitraty number of inputs
    for(let i = 0; i < argument.length; i++){
        let rng = new PRNG(arguments[i]);
        let num = rng.get();
    }
}

class PerlinField{
    constructor(seed){
        this.seed =
    }
}